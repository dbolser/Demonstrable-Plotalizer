# Vite Environment Variables
# Copy this file to .env or .env.local for local development
# Note: Variables prefixed with VITE_ are exposed to the client-side code

# Comma-separated list of allowed hosts for the preview server
# Example: VITE_ALLOWED_HOSTS=outsee3,192.168.1.33,myserver.local
VITE_ALLOWED_HOSTS=

# Optional: Gemini API Key (if using AI features)
# GEMINI_API_KEY=your_api_key_here
